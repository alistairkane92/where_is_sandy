import React from 'react';

const LoadErrorMsg = () => {
  return(
    <div id="load-error-msg" className="center">
    <p>(LoadError): cannot load such file</p>
    </div>
  )
}

export default LoadErrorMsg;
